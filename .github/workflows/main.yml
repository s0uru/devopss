name: CI-Project-Check
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and verify with Docker Compose
        # Ta jedna komenda sprawdzi czy Dockerfile buduje się poprawnie 
        # oraz czy plik docker-compose.yml jest dobrze sformatowany.
        run: docker compose build
